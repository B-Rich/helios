require 'spec_helper'

describe Helios::Bot do
  it 'has a version number' do
    expect(Helios::Bot::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
